## 1. 連線容量模型與檢查

- [x] 1.1 在下載端連線管理模組定義方向性上限常數（upload=2、download=2）與對應狀態結構
- [x] 1.2 實作連線建立前的容量檢查函式，依方向判斷是否允許建立新連線

## 2. 連線生命週期整合

- [x] 2.1 將容量檢查接入既有 WebRTC 連線建立入口，達上限時拒絕同向新連線
- [x] 2.2 在連線結束（完成、失敗、逾時、主動關閉）時統一回收對應方向名額

## 3. 中控分派條件細化

- [x] 3.1 在中控配對掃描邏輯加入來源端 `upload < 2` 與目標端 `download < 2` 的前置檢查
- [x] 3.2 僅在目標端缺少來源端片段時才建立下載端互傳分派，並保留既有排序原則

## 4. 相容性與驗證

- [x] 4.1 新增/更新測試案例，覆蓋「未達上限可建立」、「達上限拒絕建立」與「中控因容量不足不分派」情境
- [x] 4.2 驗證既有傳輸流程（背壓、CRC32、事件回報、逾時）在上限與新分派條件下行為不變
