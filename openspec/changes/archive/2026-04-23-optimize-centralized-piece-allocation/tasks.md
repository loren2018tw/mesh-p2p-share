## 1. 片段稀缺性計算

- [x] 1.1 在 `find_and_assign_matches` 中，對每個檔案的所有片段計算持有者數量（遍歷所有端點的 `owned_chunks`）
- [x] 1.2 將片段依持有者數量升冪排序，產生本輪配對的候選片段清單
- [x] 1.3 補齊日誌，記錄被挑選片段的索引、稀缺度（持有者數）與目標端點持有量

## 2. 以片段為中心的配對迴圈

- [x] 2.1 改寫 `find_and_assign_matches` 主迴圈：以片段清單（而非端點清單）為外層迴圈，逐一從最稀缺片段嘗試配對
- [x] 2.2 對每個片段，找出缺少該片段且下載容量未滿的端點集合，若多個則選已持有片段數最少者
- [x] 2.3 對每個片段，找出持有該片段且上傳容量未滿的來源端點，若多個則選上傳連線數最低者
- [x] 2.4 若兩個集合都非空則完成指派，並將該片段記入 `assigned_this_round` 防止本輪重複分配；否則繼續下一個片段

## 3. 驗證與回歸檢查

- [x] 3.1 驗證三個下載端同時啟動時，分享端在同一輪分配到的片段各不相同
- [x] 3.2 驗證下載端互傳時優先處理稀缺片段，而非依索引順序
- [x] 3.3 執行 Rust 編譯檢查與手動情境測試，確認無回歸
